;; Generated by 🤖
(module
  ;; Import a function from the "env" module named "add"
  (func $add (import "env" "add") (param i32 i32) (result i32))
  
  ;; Import a global from the "env" module named "factor"
  (global $factor (import "env" "factor") i32)
  
  ;; Regular function that uses the imported function and global
  (func $multiply (export "multiply") (param $a i32) (param $b i32) (result i32)
    ;; First add the two numbers using the imported function
    local.get $a
    local.get $b
    call $add
    ;; Then multiply by the imported global factor
    global.get $factor
    i32.mul
  )
)
