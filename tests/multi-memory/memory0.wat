(module
  ;; Import memory instances
  (import "" "memory0" (memory 1))

  ;; Function to store a value in memory
  (func $store (param $addr i32) (param $value i32)
    local.get $addr
    local.get $value
    i32.store
  )

  ;; Function to load a value from memory
  (func $load (param $addr i32) (result i32)
    local.get $addr
    i32.load
  )

  ;; Export the functions
  (export "store" (func $store))
  (export "load" (func $load))
) ;; Generated by 🤖