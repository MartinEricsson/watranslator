// Generated by 🤖
import { peekToken, atEnd, skipToken, getToken, getCurrentCursor } from './tape.mjs';
import { parseInstruction } from './parse-instruction.mjs';
import { createError } from './parse-util.mjs';

function isParamType(tokenValue) {
    if (!tokenValue) return false;
    return tokenValue === 'i32' ||
        tokenValue === 'i64' ||
        tokenValue === 'f32' ||
        tokenValue === 'f64' ||
        tokenValue === 'v128' ||
        tokenValue === 'anyfunc' ||
        tokenValue === 'funcref' ||
        tokenValue === 'externref';
}

export function parseFunction() {
    let funcName = null; // Stores the string name
    let funcNameLoc = null; // Stores { line, column } for the name token
    const params = []; // Array of { name, type, loc }
    const results = []; // Array of type strings, or { value: typeString, loc }
    const locals = []; // Array of { name, type, loc }
    const instructions = [];
    const blockStack = []; 
    let exportInfo = null; // { value: "exportName", loc: locOfExportKeyword }
    let typeRefInfo = null; // { value: "$typeName", loc: locOfTypeKeyword }
    let importDetails = null; // { module, field, loc: locOfImportKeyword }

    const blockLabels = new Map();

    // Check for function name (e.g., $main)
    let nextToken = peekToken();
    if (nextToken && nextToken.value.startsWith('$')) {
        const nameToken = getToken();
        funcName = nameToken.value;
        funcNameLoc = { line: nameToken.line, column: nameToken.column };
        nextToken = peekToken(); // Update nextToken
    }

    // Parse parameters, results, locals and body
    // Loop while the next token is not the closing parenthesis of the function definition
    while (nextToken && nextToken.value !== ')') {
        if (nextToken.value === '(') {
            skipToken(); // Consume '('

            const keywordToken = getToken(); // This is 'param', 'result', 'local', 'type', 'export', 'import'
            if (!keywordToken) throw createError("Unexpected end of input after '('.");
            
            const keywordLoc = { line: keywordToken.line, column: keywordToken.column };

            switch (keywordToken.value) {
                case 'type':
                    // (type $typeName)
                    const typeNameToken = getToken();
                    if (!typeNameToken) throw createError("Expected type name after (type.", keywordLoc.line, keywordLoc.column);
                    typeRefInfo = { 
                        value: typeNameToken.value, 
                        loc: { line: typeNameToken.line, column: typeNameToken.column },
                        keywordLoc // loc of 'type' keyword
                    };
                    break;

                case 'param':
                    // (param $name type) or (param type) or (param type type)
                    let pNameToken, pTypeToken;
                    let pName = params.length.toString(); // Default name
                    let pNameLoc = keywordLoc; // Default loc for name is param keyword's loc

                    let nextParamToken = peekToken();
                    if (nextParamToken && nextParamToken.value.startsWith('$')) {
                        pNameToken = getToken();
                        pName = pNameToken.value;
                        pNameLoc = { line: pNameToken.line, column: pNameToken.column };
                        nextParamToken = peekToken();
                    }

                    while(nextParamToken && isParamType(nextParamToken.value)) {
                        pTypeToken = getToken();
                        params.push({ 
                            name: pName, // If multiple types for one name, name will be reused. WAT allows this.
                            type: pTypeToken.value, 
                            loc: { line: pTypeToken.line, column: pTypeToken.column }, // loc of the type
                            nameLoc: pName === params.length.toString() ? keywordLoc : pNameLoc, // loc of name or param keyword
                            keywordLoc // loc of 'param' keyword
                        });
                        // For subsequent types under same (param $name t1 t2), they get new default names
                        if (pNameToken) { 
                            pName = params.length.toString();
                            pNameLoc = keywordLoc;
                        }
                        nextParamToken = peekToken();
                    }
                    break;

                case 'result':
                    // (result type1 type2 ...)
                    let nextResultToken = peekToken();
                    while (nextResultToken && nextResultToken.value !== ')') {
                        const typeToken = getToken();
                        results.push({ 
                            value: typeToken.value, 
                            loc: { line: typeToken.line, column: typeToken.column } 
                        });
                        nextResultToken = peekToken();
                    }
                    break;

                case 'local':
                    // (local $name type) or (local type)
                    let lNameToken, lTypeToken;
                    let lName = locals.length.toString(); // Default name
                    let lNameLoc = keywordLoc; // Default loc

                    let nextLocalToken = peekToken();
                    if (nextLocalToken && nextLocalToken.value.startsWith('$')) {
                        lNameToken = getToken();
                        lName = lNameToken.value;
                        lNameLoc = { line: lNameToken.line, column: lNameToken.column };
                        nextLocalToken = peekToken();
                    }
                    
                    // WAT allows (local type1 type2...) similar to param, handle this
                    while(nextLocalToken && isParamType(nextLocalToken.value)) {
                        lTypeToken = getToken();
                        locals.push({ 
                            name: lName, 
                            type: lTypeToken.value, 
                            loc: { line: lTypeToken.line, column: lTypeToken.column }, // loc of the type
                            nameLoc: lName === locals.length.toString() ? keywordLoc : lNameLoc, // loc of name or local keyword
                            keywordLoc // loc of 'local' keyword
                        });
                        if (lNameToken) { // If there was an explicit name, subsequent types are anonymous
                           lName = locals.length.toString();
                           lNameLoc = keywordLoc;
                        }
                        nextLocalToken = peekToken();
                    }
                    break;

                case 'export':
                    // (export "name")
                    const exportNameToken = getToken();
                    if (!exportNameToken || !exportNameToken.value.startsWith('"') || !exportNameToken.value.endsWith('"')) {
                        throw createError('Expected string for export name', keywordLoc.line, keywordLoc.column);
                    }
                    exportInfo = { 
                        value: exportNameToken.value.substring(1, exportNameToken.value.length - 1), 
                        loc: { line: exportNameToken.line, column: exportNameToken.column }, // loc of "name"
                        keywordLoc // loc of 'export' keyword
                    };
                    break;

                case 'import':
                    // (import "module" "field")
                    const moduleNameToken = getToken();
                    if (!moduleNameToken || !moduleNameToken.value.startsWith('"') || !moduleNameToken.value.endsWith('"')) {
                        throw createError('Expected string for import module name', keywordLoc.line, keywordLoc.column);
                    }
                    const fieldNameToken = getToken();
                    if (!fieldNameToken || !fieldNameToken.value.startsWith('"') || !fieldNameToken.value.endsWith('"')) {
                        throw createError('Expected string for import field name', 
                            moduleNameToken ? moduleNameToken.line : keywordLoc.line, 
                            moduleNameToken ? moduleNameToken.column + moduleNameToken.value.length : keywordLoc.column);
                    }
                    importDetails = {
                        module: moduleNameToken.value.substring(1, moduleNameToken.value.length - 1),
                        moduleLoc: {line: moduleNameToken.line, column: moduleNameToken.column },
                        field: fieldNameToken.value.substring(1, fieldNameToken.value.length - 1),
                        fieldLoc: {line: fieldNameToken.line, column: fieldNameToken.column },
                        keywordLoc // loc of 'import' keyword
                    };
                    break;
                default:
                    // Skip other expressions like (data ...), (elem ...) if they appear unexpectedly here
                    // Though valid WAT structure usually places these at module level or specific instructions
                    console.warn(`Unexpected keyword '${keywordToken.value}' in function signature at line ${keywordLoc.line}, col ${keywordLoc.column}. Skipping.`);
                    let depth = 1;
                    while (!atEnd()) {
                        const t = getToken();
                        if (t.value === '(') depth++;
                        else if (t.value === ')') depth--;
                        if (depth === 0) break;
                    }
                    break;
            }
            // Ensure the closing parenthesis of the s-expression (e.g. (param type)) is consumed
            const closingParen = peekToken();
            if (closingParen && closingParen.value === ')') {
                skipToken(); // Consume ')'
            } else {
                throw createError(`Expected ')' to close s-expression starting with '${keywordToken.value}' at line ${keywordLoc.line}, column ${keywordLoc.column}, but got ${closingParen ? closingParen.value : "EOF"}`);
            }
        } else { // Instruction parsing part
            const instructionToken = peekToken(); // This is the instruction itself e.g. 'block', 'loop', 'if', 'i32.const'
            const instructionLoc = instructionToken ? { line: instructionToken.line, column: instructionToken.column } : getCurrentCursor(); // Fallback, though should always have token

            if (instructionToken.value === 'block' || instructionToken.value === 'loop' || instructionToken.value === 'if') {
                skipToken(); // Consume 'block', 'loop', or 'if' keyword
                
                let label = null, labelLoc = null;
                let resultType = null, resultTypeLoc = null; // Can be single or array of {value, loc}

                // Parse block/loop label if present (e.g., $L1)
                let nextInstrToken = peekToken();
                if (nextInstrToken && nextInstrToken.value.startsWith('$')) {
                    const labelToken = getToken();
                    label = labelToken.value;
                    labelLoc = { line: labelToken.line, column: labelToken.column };
                    blockLabels.set(label, blockStack.length); // Keep original label tracking
                    nextInstrToken = peekToken();
                }

                // Parse result type e.g. (result i32)
                if (nextInstrToken && nextInstrToken.value === '(') {
                    skipToken(); // Consume '('
                    const resultKeywordToken = getToken(); // Should be 'result'
                    if (!resultKeywordToken || resultKeywordToken.value !== 'result') {
                        throw createError(`Expected '(result ...)' but got '(${resultKeywordToken ? resultKeywordToken.value : ""}'`, 
                                          nextInstrToken.line, nextInstrToken.column);
                    }
                    const resultTypesList = [];
                    let typeToken = peekToken();
                    while(typeToken && typeToken.value !== ')') {
                        const consumedTypeToken = getToken();
                        resultTypesList.push({ value: consumedTypeToken.value, loc: { line: consumedTypeToken.line, column: consumedTypeToken.column }});
                        typeToken = peekToken();
                    }
                    if (resultTypesList.length === 1) {
                        resultType = resultTypesList[0].value; // Keep it simple if single, but loc is on list item
                        resultTypeLoc = resultTypesList[0].loc; 
                    } else if (resultTypesList.length > 1) {
                        resultType = resultTypesList.map(rt => rt.value); // Array of strings
                        // loc for multi-value might be the loc of 'result' keyword or span of types
                        resultTypeLoc = {line: resultKeywordToken.line, column: resultKeywordToken.column}; 
                    }

                    const closingResultParen = getToken(); // Consume ')'
                    if (!closingResultParen || closingResultParen.value !== ')') {
                         throw createError(`Expected ')' to close result type declaration, but got '${closingResultParen ? closingResultParen.value : "EOF"}'`,
                                           resultKeywordToken.line, resultKeywordToken.column);
                    }
                }
                
                const structuredInstr = {
                    type: instructionToken.value, // 'block', 'loop', or 'if'
                    label: label, // string or null
                    labelLoc: labelLoc, // {line, col} or null
                    resultType: resultType, // string, array of strings, or null
                    resultTypeLoc: resultTypeLoc, // {line, col} or null for result keyword or specific type
                    instructions: instructionToken.value !== 'if' ? [] : undefined, // 'if' uses then/else
                    thenInstructions: instructionToken.value === 'if' ? [] : undefined,
                    elseInstructions: instructionToken.value === 'if' ? [] : undefined,
                    inElseBranch: instructionToken.value === 'if' ? false : undefined,
                    loc: instructionLoc, // loc of 'block', 'loop', 'if' keyword
                };

                if (blockStack.length > 0) {
                    const parentBlock = blockStack[blockStack.length - 1];
                     if (parentBlock.type === 'if') {
                        if (parentBlock.inElseBranch) parentBlock.elseInstructions.push(structuredInstr);
                        else parentBlock.thenInstructions.push(structuredInstr);
                    } else {
                        parentBlock.instructions.push(structuredInstr);
                    }
                } else {
                    instructions.push(structuredInstr);
                }
                blockStack.push(structuredInstr);

            } else if (instructionToken.value === 'else') {
                skipToken(); // Consume 'else'
                if (blockStack.length > 0) {
                    const currentBlock = blockStack[blockStack.length - 1];
                    if (currentBlock.type === 'if') {
                        currentBlock.inElseBranch = true;
                        currentBlock.elseLoc = instructionLoc; // Add loc for 'else' keyword
                    } else {
                         throw createError("'else' outside of 'if' block", instructionLoc.line, instructionLoc.column);
                    }
                } else {
                     throw createError("'else' outside of 'if' block", instructionLoc.line, instructionLoc.column);
                }
            } else if (instructionToken.value === 'end') {
                skipToken(); // Consume 'end'
                if (blockStack.length > 0) {
                    const endedBlock = blockStack.pop();
                    endedBlock.endLoc = instructionLoc; // Add loc for 'end' keyword
                } else {
                    // This 'end' might be for the function itself if not inside a block
                    // Or it's an error if too many 'end's
                }
            } else {
                // Regular instruction (e.g., i32.const, local.get)
                // parseInstruction should handle setting its own loc based on the instruction token
                const instr = parseInstruction(blockLabels); // parseInstruction needs to be updated to use peekToken for its loc

                if (blockStack.length > 0) {
                    const currentBlock = blockStack[blockStack.length - 1];
                    if (currentBlock.type === 'if') {
                        if (currentBlock.inElseBranch) currentBlock.elseInstructions.push(instr);
                        else currentBlock.thenInstructions.push(instr);
                    } else {
                        currentBlock.instructions.push(instr);
                    }
                } else {
                    instructions.push(instr);
                }
            }
        }
        nextToken = peekToken(); // Update nextToken for the loop condition
    }


    // Return function object with additional inline export information
    const func = {
        name: funcName,
        nameLoc: funcNameLoc,
        parameters: params,
        locals: locals,
        results: results, // results now contain {value, loc} objects
        instructions: instructions,
        typeRef: typeRefInfo,  // Include the type reference if present {value, loc, keywordLoc}
        export: exportInfo, // {value, loc, keywordLoc}
        import: importDetails // {module, moduleLoc, field, fieldLoc, keywordLoc}
    };

    return func;
}
                } else {
                    // Skip other expressions
                    while (!atEnd() && peekToken() !== ')') {
                        skipToken()
                    }
                }
            }

            // Skip the closing paren
            if (!atEnd() && peekToken() === ')') {
                skipToken()
            }
        } else if (peekToken() === 'block') {
            skipToken() // Skip 'block' keyword

            let label = null;
            let resultType = null;

            // Parse block label if present
            if (!atEnd() && peekToken().startsWith('$')) {
                label = getToken();

                // Store the label with its nesting depth for branch resolution
                blockLabels.set(label, blockStack.length);
            }

            // Generated by 🤖
            // Parse result type if present
            if (!atEnd() && peekToken() === '(') {
                skipToken() // Skip opening paren
                if (!atEnd() && peekToken() === 'result') {
                    skipToken() // Skip 'result' keyword

                    // Collect all result types
                    const resultTypes = [];
                    while (!atEnd() && peekToken() !== ')') {
                        resultTypes.push(getToken());
                    }

                    // If there's only one result type, store it as a string
                    // Otherwise, store as an array of types for multi-value returns
                    if (resultTypes.length === 1) {
                        resultType = resultTypes[0];
                    } else if (resultTypes.length > 1) {
                        resultType = resultTypes;
                    }

                    // Skip closing paren of result type
                    if (!atEnd() && peekToken() === ')') {
                        skipToken()
                    }
                }
            }

            const blockInstr = {
                type: 'block',
                label: label,
                resultType: resultType,
                instructions: []
            };

            // Add this block to its parent's instructions or the main function body
            if (blockStack.length > 0) {
                const parentBlock = blockStack[blockStack.length - 1];
                parentBlock.instructions.push(blockInstr);
            } else {
                // Add to main function body
                instructions.push(blockInstr);
            }

            // Push this block to the stack to collect its instructions
            blockStack.push(blockInstr);
        } else if (peekToken() === 'loop') {
            skipToken() // Skip 'loop' keyword

            let label = null;
            let resultType = null;

            // Parse loop label if present
            if (!atEnd() && peekToken().startsWith('$')) {
                label = getToken();

                // Store the label with its nesting depth for branch resolution
                blockLabels.set(label, blockStack.length);
            }

            // Parse result type if present
            if (!atEnd() && peekToken() === '(') {
                skipToken() // Skip opening paren
                if (!atEnd() && peekToken() === 'result') {
                    skipToken() // Skip 'result' keyword

                    // Generated by 🤖
                    // Collect all result types
                    const resultTypes = [];
                    while (!atEnd() && peekToken() !== ')') {
                        resultTypes.push(getToken());
                    }

                    // If there's only one result type, store it as a string
                    // Otherwise, store as an array of types for multi-value returns
                    if (resultTypes.length === 1) {
                        resultType = resultTypes[0];
                    } else if (resultTypes.length > 1) {
                        resultType = resultTypes;
                    }

                    // Skip closing paren of result type
                    if (!atEnd() && peekToken() === ')') {
                        skipToken()
                    }
                }
            }

            const loopInstr = {
                type: 'loop',
                label: label,
                resultType: resultType,
                instructions: []
            };

            // Add this loop to its parent's instructions or the main function body
            if (blockStack.length > 0) {
                const parentBlock = blockStack[blockStack.length - 1];
                parentBlock.instructions.push(loopInstr);
            } else {
                // Add to main function body
                instructions.push(loopInstr);
            }

            // Push this loop to the stack to collect its instructions
            blockStack.push(loopInstr);
        } else if (peekToken() === 'if') {
            skipToken() // Skip 'if' keyword

            let resultType = null;

            // Generated by 🤖
            // Check for result type, handling multi-value results
            if (!atEnd() && peekToken() === '(') {
                skipToken() // Skip opening paren
                if (!atEnd() && peekToken() === 'result') {
                    skipToken() // Skip 'result' keyword

                    // Collect all result types
                    const resultTypes = [];
                    while (!atEnd() && peekToken() !== ')') {
                        resultTypes.push(getToken());
                    }

                    // If there's only one result type, store it as a string
                    // Otherwise, store as an array of types for multi-value returns
                    if (resultTypes.length === 1) {
                        resultType = resultTypes[0];
                    } else if (resultTypes.length > 1) {
                        resultType = resultTypes;
                    }

                    // Skip closing paren of result type
                    if (!atEnd() && peekToken() === ')') {
                        skipToken()
                    }
                }
            }

            const ifInstr = {
                type: 'if',
                resultType: resultType,
                thenInstructions: [],
                elseInstructions: [],
                inElseBranch: false
            };

            // Add this if to its parent's instructions or the main function body
            if (blockStack.length > 0) {
                const parentBlock = blockStack[blockStack.length - 1];
                parentBlock.instructions.push(ifInstr);
            } else {
                // Add to main function body
                instructions.push(ifInstr);
            }

            // Push this if block to the stack to collect its instructions
            blockStack.push(ifInstr);
        } else if (peekToken() === 'else') {
            // Handle else branch - switch current if block to collect else instructions
            if (blockStack.length > 0) {
                const currentBlock = blockStack[blockStack.length - 1];
                if (currentBlock.type === 'if') {
                    currentBlock.inElseBranch = true;
                }
            }
            skipToken()
        } else if (peekToken() === 'end') {
            // Handle end of a block - pop the current block from the stack
            if (blockStack.length > 0) {
                blockStack.pop();
            }
            skipToken()
        } else {
            // Parse other instructions (local.get, i32.add, etc.)
            const instr = parseInstruction(blockLabels);

            // Add the instruction to the current block or function body
            if (blockStack.length > 0) {
                const currentBlock = blockStack[blockStack.length - 1];
                if (currentBlock.type === 'if') {
                    if (currentBlock.inElseBranch) {
                        currentBlock.elseInstructions.push(instr);
                    } else {
                        currentBlock.thenInstructions.push(instr);
                    }
                } else {
                    // For blocks and loops
                    currentBlock.instructions.push(instr);
                }
            } else {
                // Add to main function body
                instructions.push(instr);
            }
        }
    }

    // Return function object with additional inline export information
    const func = {
        name: name,
        parameters: params,
        locals: locals,
        results: results,
        instructions: instructions,
        typeRef: typeRef  // Include the type reference if present
    };

    // Generated by 🤖
    // If this function has an inline export, include it in the return value
    if (exportName) {
        func.export = exportName;
    }

    // If this function has an import, include it in the return value
    if (importInfo) {
        func.import = importInfo;
    }

    return func;
}