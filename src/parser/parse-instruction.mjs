// Generated by 🤖
import { peekToken, skipToken, atEnd, getCurrentCursor, getToken } from './tape.mjs';
import { isLabel } from './parse-util.mjs';
import { parseMemoryOpsInstruction } from './instruction/parse-memory-ops-instruction.mjs';
import { parseTableInstruction } from './instruction/parse-table-instruction.mjs';
import { parseAtomicRMWInstruction } from './instruction/parse-atomic-rmw-instruction.mjs';
import { parseRefInstruction } from './instruction/parse-ref-instruction.mjs';
import { parseAtomicsOpInstruction } from './instruction/parse-atomics-op-instruction.mjs';
import { parseSIMDLaneBased } from './instruction/parse-simd-lane-based.mjs';
import { parseSIMDMemory } from './instruction/parse-simd-memory.mjs';
import { parseSIMDPlainOpcode } from './instruction/parse-simd-plain-opcode.mjs';

const stackInstructions = [
    // Value types that might appear as standalone tokens in multi-value returns
    'i32', 'i64', 'f32', 'f64', 'v128', 'funcref',

    // Memory and table operations
    'memory.fill', 'memory.copy', 'memory.init', 'memory.size', 'memory.grow',
    'elem.drop', 'table.init', 'table.drop', 'table.copy',

    // Atomic operations
    'memory.atomic.notify', 'memory.atomic.wait32', 'memory.atomic.wait64',
    'i32.atomic.load', 'i32.atomic.load8_u', 'i32.atomic.load16_u',
    'i64.atomic.load', 'i64.atomic.load8_u', 'i64.atomic.load16_u', 'i64.atomic.load32_u',
    'i32.atomic.store', 'i32.atomic.store8', 'i32.atomic.store16',
    'i64.atomic.store', 'i64.atomic.store8', 'i64.atomic.store16', 'i64.atomic.store32',
    'i32.atomic.rmw.add', 'i64.atomic.rmw.add',
    'i32.atomic.rmw.sub', 'i64.atomic.rmw.sub',
    'i32.atomic.rmw.and', 'i64.atomic.rmw.and',
    'i32.atomic.rmw.or', 'i64.atomic.rmw.or',
    'i32.atomic.rmw.xor', 'i64.atomic.rmw.xor',
    'i32.atomic.rmw.xchg', 'i64.atomic.rmw.xchg',
    'i32.atomic.rmw.cmpxchg', 'i64.atomic.rmw.cmpxchg',
    'atomic.fence', // Generated by 🤖

    // i32 instructions
    'nop', 'return', 'drop', 'unreachable',
    'i32.add', 'i32.sub', 'i32.mul', 'i32.div_s', 'i32.div_u',
    'i32.rem_s', 'i32.rem_u', 'i32.and', 'i32.or', 'i32.xor',
    'i32.shl', 'i32.shr_s', 'i32.shr_u', 'i32.rotl', 'i32.rotr',
    'i32.eq', 'i32.ne', 'i32.lt_s', 'i32.lt_u', 'i32.gt_s',
    'i32.gt_u', 'i32.le_s', 'i32.le_u', 'i32.ge_s', 'i32.ge_u',
    'i32.clz', 'i32.ctz', 'i32.popcnt', 'i32.eqz', 'i32.wrap_i64',
    'i32.trunc_f32_s', 'i32.trunc_f32_u', 'i32.trunc_f64_s', 'i32.trunc_f64_u',
    'i32.reinterpret_f32', 'i32.trunc_sat_f32_s', 'i32.trunc_sat_f32_u',
    'i32.trunc_sat_f64_s', 'i32.trunc_sat_f64_u', 'i32.extend8_s', 'i32.extend16_s',
    // f32 instructions
    'f32.add', 'f32.sub', 'f32.mul', 'f32.div',
    'f32.abs', 'f32.neg', 'f32.sqrt', 'f32.min', 'f32.max',
    'f32.ceil', 'f32.floor', 'f32.trunc', 'f32.nearest', 'f32.copysign',
    'f32.eq', 'f32.ne', 'f32.lt', 'f32.gt', 'f32.le', 'f32.ge',
    'f32.convert_i32_s', 'f32.convert_i32_u', 'f32.convert_i64_s', 'f32.convert_i64_u',
    'f32.demote_f64', 'f32.reinterpret_i32',
    'i32.lt_s', 'i32.gt_s', 'i32.le_s', 'i32.ge_s',
    // i64 instructions
    'i64.add', 'i64.sub', 'i64.mul', 'i64.div_s', 'i64.div_u',
    'i64.rem_s', 'i64.rem_u', 'i64.and', 'i64.or', 'i64.xor',
    'i64.shl', 'i64.shr_s', 'i64.shr_u', 'i64.rotl', 'i64.rotr',
    'i64.eq', 'i64.ne', 'i64.lt_s', 'i64.lt_u', 'i64.gt_s',
    'i64.gt_u', 'i64.le_s', 'i64.le_u', 'i64.ge_s', 'i64.ge_u',
    'i64.clz', 'i64.ctz', 'i64.popcnt', 'i64.eqz', 'i64.extend8_s', 'i64.extend16_s', 'i64.extend32_s',
    'i64.trunc_f32_s', 'i64.trunc_f32_u', 'i64.trunc_f64_s', 'i64.trunc_f64_u', 'i64.reinterpret_f64',
    'i64.extend_i32_s', 'i64.extend_i32_u', 'i64.load', 'i64.trunc_sat_f32_s',
    'i64.trunc_sat_f32_u', 'i64.trunc_sat_f64_s', 'i64.trunc_sat_f64_u',
    // Add f64 operations
    'f64.add', 'f64.sub', 'f64.mul', 'f64.div',
    'f64.abs', 'f64.neg', 'f64.sqrt', 'f64.min', 'f64.max',
    'f64.ceil', 'f64.floor', 'f64.trunc', 'f64.nearest', 'f64.copysign',
    'f64.eq', 'f64.ne', 'f64.lt', 'f64.gt', 'f64.le', 'f64.ge',
    'f64.convert_i32_s', 'f64.convert_i32_u', 'f64.convert_i64_s', 'f64.convert_i64_u',
    'f64.promote_f32', 'f64.reinterpret_i64',
    'ref.is_null' // Generated by 🤖
];

export function parseInstruction(blockLabels) {
    const instructionTokenObj = peekToken(); 
    if (!instructionTokenObj) {
        // This case should ideally be handled by the caller (e.g. parseFunction) checking atEnd()
        // or by parseFunction expecting a closing ')' before EOF.
        // Throw an error or return null if appropriate for your parser's design.
        throw new Error("Unexpected end of input when expecting an instruction.");
    }
    const loc = { line: instructionTokenObj.line, column: instructionTokenObj.column };
    const opCode = instructionTokenObj.value; // The string opcode
    skipToken(); // Consume instruction token

    // Pass the token object and its loc to sub-parsers
    const refInstruction = parseRefInstruction(instructionTokenObj, loc);
    if (refInstruction) { // sub-parsers should return node with loc
        return refInstruction;
    }

    const tableInstr = parseTableInstruction(instructionTokenObj, loc);
    if (tableInstr) {
        return tableInstr;
    }

    const memoryOps = parseMemoryOpsInstruction(instructionTokenObj, loc);
    if (memoryOps) {
        return memoryOps;
    }

    const atomicRMWInstr = parseAtomicRMWInstruction(instructionTokenObj, loc);
    if (atomicRMWInstr) {
        return atomicRMWInstr;
    }

    const atomicOps = parseAtomicsOpInstruction(instructionTokenObj, loc);
    if (atomicOps) {
        return atomicOps;
    }

    const simdLaneInstr = parseSIMDLaneBased(instructionTokenObj, loc);
    if (simdLaneInstr) {
        return simdLaneInstr;
    }

    const simdMemoryInstr = parseSIMDMemory(instructionTokenObj, loc);
    if (simdMemoryInstr) {
        return simdMemoryInstr;
    }

    const simdPlainInstr = parseSIMDPlainOpcode(instructionTokenObj, loc);
    if (simdPlainInstr) {
        return simdPlainInstr;
    }

    // Continue with the original parseInstruction logic for other instructions
    // opCode is instructionTokenObj.value
    if (stackInstructions.includes(opCode)) {
        // Handle stack instructions
        return { type: opCode, loc: loc };
    }
    if (opCode === 'select') {
        // Handle select instruction explicitly
        // Check if this is a typed select with a result type
        if (!atEnd() && peekToken() === '(') {
            skipToken() // Skip opening paren

            // Check for result type
            if (!atEnd() && peekToken() === 'result') {
                skipToken() // Skip 'result' keyword

                // Get the result type
                if (!atEnd()) {
                    const resultTypeToken = getToken(); // This is a token object

                    // Skip closing paren
                    if (!atEnd() && peekToken().value === ')') {
                        skipToken();
                        // Return typed select instruction
                        return { type: 'select_t', resultType: resultTypeToken.value, resultTypeLoc: {line: resultTypeToken.line, column: resultTypeToken.column}, loc: loc };
                    }
                }
            }

            // If we couldn't parse a result type, skip to end of paren
            while (!atEnd() && peekToken().value !== ')') {
                skipToken();
            }
            if (!atEnd() && peekToken().value === ')') {
                skipToken();
            }
        }

        // Regular select instruction (no explicit type)
        return { type: 'select', loc: loc };
    }

    let operandToken, operandValue, operandLoc;

    if (opCode === 'local.get' || opCode === 'get_local') {
        operandToken = getToken();
        operandValue = operandToken.value;
        operandLoc = { line: operandToken.line, column: operandToken.column };
        return { type: 'get_local', operand: operandValue, operandLoc, loc: loc };
    }

    if (opCode === 'local.set' || opCode === 'set_local') {
        operandToken = getToken();
        operandValue = operandToken.value;
        operandLoc = { line: operandToken.line, column: operandToken.column };
        return { type: 'set_local', operand: operandValue, operandLoc, loc: loc };
    }

    if (opCode === 'local.tee') {
        operandToken = getToken();
        operandValue = operandToken.value;
        operandLoc = { line: operandToken.line, column: operandToken.column };
        return { type: 'local.tee', operand: operandValue, operandLoc, loc: loc };
    }

    if (opCode === 'global.get' || opCode === 'get_global') {
        operandToken = getToken();
        operandValue = operandToken.value;
        operandLoc = { line: operandToken.line, column: operandToken.column };
        return { type: 'global.get', operand: operandValue, operandLoc, loc: loc };
    }

    if (opCode === 'global.set' || opCode === 'set_global') {
        operandToken = getToken();
        operandValue = operandToken.value;
        operandLoc = { line: operandToken.line, column: operandToken.column };
        return { type: 'global.set', operand: operandValue, operandLoc, loc: loc };
    }

    if (opCode === 'i32.const') {
        const valueToken = getToken();
        const valueStr = valueToken.value;
        let value;
        if (valueStr.toLowerCase().startsWith('0x')) {
            value = Number.parseInt(valueStr, 16);
        } else {
            value = Number.parseInt(valueStr, 10);
        }
        value = value | 0; 
        return { type: 'i32.const', value, valueLoc: {line: valueToken.line, column: valueToken.column}, loc: loc };
    }

    if (opCode === 'i64.const') {
        const valueToken = getToken();
        const valueStr = valueToken.value;
        let value = BigInt(valueStr);
        value = value & BigInt("0xFFFFFFFFFFFFFFFF");
        if (value > BigInt("0x7FFFFFFFFFFFFFFF")) {
            value = value - BigInt("0x10000000000000000");
        }
        return { type: 'i64.const', value, valueLoc: {line: valueToken.line, column: valueToken.column}, loc: loc };
    }

    if (opCode === 'f32.const') {
        const valueToken = getToken();
        const value = Number.parseFloat(valueToken.value);
        return { type: 'f32.const', value, valueLoc: {line: valueToken.line, column: valueToken.column}, loc: loc };
    }

    if (opCode === 'f64.const') {
        const valueToken = getToken();
        const value = Number.parseFloat(valueToken.value);
        return { type: 'f64.const', value, valueLoc: {line: valueToken.line, column: valueToken.column}, loc: loc };
    }

    if (opCode === 'br') {
        const labelToken = getToken();
        return { type: 'br', label: labelToken.value, labelLoc: {line: labelToken.line, column: labelToken.column}, loc: loc };
    }

    if (opCode === 'br_if') {
        const labelToken = getToken();
        const labelValue = labelToken.value;
        const branchInstr = {
            type: 'br_if',
            label: labelValue,
            labelLoc: {line: labelToken.line, column: labelToken.column},
            labelDepth: blockLabels.has(labelValue) ? blockLabels.get(labelValue) : null,
            loc: loc
        };
        return branchInstr;
    }

    if (opCode === 'br_table') {
        const labels = []; // Array of { value: labelStr, loc: labelLoc }
        let defaultLabelInfo = null; // { value: labelStr, loc: labelLoc }

        let nextLabelToken = peekToken();
        while (nextLabelToken && (isLabel(nextLabelToken.value) || nextLabelToken.value.startsWith('$'))) {
            const labelToken = getToken();
            labels.push({ value: labelToken.value, loc: { line: labelToken.line, column: labelToken.column } });
            nextLabelToken = peekToken();
        }

        // The last label parsed is the default label
        if (labels.length > 0) {
            defaultLabelInfo = labels.pop();
        }
        
        return {
            type: 'br_table',
            labels: labels.map(l => l.value), // array of string label values
            labelsInfo: labels, // array of {value, loc} objects
            defaultLabel: defaultLabelInfo ? defaultLabelInfo.value : null,
            defaultLabelInfo: defaultLabelInfo,
            loc: loc
        };
    }
    
    let dataLabelToken, dataLabelValue, dataLabelLoc;
    if (opCode === 'data.drop') {
        let segmentIdx = null, segmentIdxLoc = null; // For numeric index
        const nextOpToken = peekToken();
        if (nextOpToken) {
            if (nextOpToken.value.startsWith('$')) { // Named data segment
                dataLabelToken = getToken();
                dataLabelValue = dataLabelToken.value;
                dataLabelLoc = { line: dataLabelToken.line, column: dataLabelToken.column };
            } else if (/^\d+$/.test(nextOpToken.value)) { // Numeric segment index
                segmentIdxToken = getToken();
                segmentIdx = Number.parseInt(segmentIdxToken.value, 10);
                segmentIdxLoc = { line: segmentIdxToken.line, column: segmentIdxToken.column };
            }
        }
        return { type: 'data.drop', dataLabel: dataLabelValue, dataLabelLoc, segmentIndex: segmentIdx, segmentIndexLoc: segmentIdxLoc, loc: loc };
    }
    
    let segmentIdxToken, segmentIdx, segmentIdxLoc; // For memory.init, elem.drop
    if (opCode === 'memory.init') {
        const nextMemOpToken = peekToken();
        if (nextMemOpToken && /^\d+$/.test(nextMemOpToken.value)) {
            segmentIdxToken = getToken();
            segmentIdx = Number.parseInt(segmentIdxToken.value, 10);
            segmentIdxLoc = { line: segmentIdxToken.line, column: segmentIdxToken.column };
        } else { // WASM spec says data segment index is optional for memory.init if memory 0, but text format often includes it.
             // Assuming default is 0 if not specified, or could be an error if required by context.
             // For now, let's assume it might be specified. If not, segmentIdx remains null.
        }
        return { type: 'memory.init', segmentIndex: segmentIdx, segmentIndexLoc, loc: loc };
    }

    if (opCode === 'elem.drop') {
        const nextElemOpToken = peekToken();
        if (nextElemOpToken && /^\d+$/.test(nextElemOpToken.value)) {
            segmentIdxToken = getToken(); // Re-using segmentIdx variables for element index
            segmentIdx = Number.parseInt(segmentIdxToken.value, 10);
            segmentIdxLoc = { line: segmentIdxToken.line, column: segmentIdxToken.column };
        } else {
            // Default element index is 0 if not specified
            segmentIdx = 0; 
        }
        return { type: 'elem.drop', elementIndex: segmentIdx, elementIndexLoc: segmentIdxLoc, loc: loc };
    }

    if (opCode === 'call') {
        const funcIndexToken = getToken();
        return { type: 'call', functionName: funcIndexToken.value, functionNameLoc: {line: funcIndexToken.line, column: funcIndexToken.column}, loc: loc };
    }

    if (opCode === 'call_indirect') {
        const params = []; // Array of { value: typeStr, loc: typeLoc }
        const results = []; // Array of { value: typeStr, loc: typeLoc }
        let typeRef = null, typeRefLoc = null; // For (type $name)
        let tableIndex = 0, tableIndexLoc = null; // Default to first table

        let nextCallIndirectToken = peekToken();
        while (nextCallIndirectToken && nextCallIndirectToken.value === '(') {
            skipToken(); // Skip opening paren '('

            if (!atEnd()) {
                const keywordToken = getToken(); // 'type', 'param', or 'result'

                if (keywordToken.value === 'type') {
                    const typeNameToken = getToken();
                    typeRef = typeNameToken.value;
                    typeRefLoc = { line: typeNameToken.line, column: typeNameToken.column };
                } else if (keywordToken.value === 'param') {
                    let paramTypeToken = peekToken();
                    while (paramTypeToken && paramTypeToken.value !== ')') {
                        const consumedParamTypeToken = getToken();
                        params.push({value: consumedParamTypeToken.value, loc: {line: consumedParamTypeToken.line, column: consumedParamTypeToken.column}});
                        paramTypeToken = peekToken();
                    }
                } else if (keywordToken.value === 'result') {
                    let resultTypeToken = peekToken();
                    while (resultTypeToken && resultTypeToken.value !== ')') {
                        const consumedResultTypeToken = getToken();
                        results.push({value: consumedResultTypeToken.value, loc: {line: consumedResultTypeToken.line, column: consumedResultTypeToken.column}});
                        resultTypeToken = peekToken();
                    }
                }

                if (peekToken() && peekToken().value === ')') { // Skip closing paren of (type/param/result ...)
                    skipToken();
                } else {
                    throw new Error(`Expected ')' to close ${keywordToken.value} block in call_indirect at line ${keywordToken.line}, column ${keywordToken.column}`);
                }
            }
            nextCallIndirectToken = peekToken();
        }

        // Check for table index (optional, defaults to 0)
        const tableIndexCandidateToken = peekToken();
        if (tableIndexCandidateToken && /^\d+$/.test(tableIndexCandidateToken.value)) {
            const tableIndexToken = getToken();
            tableIndex = Number.parseInt(tableIndexToken.value, 10);
            tableIndexLoc = { line: tableIndexToken.line, column: tableIndexToken.column };
        }

        return {
            type: 'call_indirect',
            params: params.map(p => p.value), // Keep simple array of strings for now
            paramsInfo: params, // Keep detailed info if needed
            results: results.map(r => r.value), // Keep simple array of strings
            resultsInfo: results,
            typeRef: typeRef,
            typeRefLoc: typeRefLoc,
            tableIndex: tableIndex,
            tableIndexLoc: tableIndexLoc,
            loc: loc
        };
    }
    // For unrecognized instructions
    const error = new Error(`Unrecognized instruction: ${opCode} at line: ${loc.line} and column: ${loc.column}`);
    error.context = {
        position: loc, // Use the loc object directly
        instruction: opCode
    };
    throw error;
}