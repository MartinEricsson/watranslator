// Generated by 🤖
import { atEnd, getToken, peekToken } from '../tape.mjs'; // Added peekToken
import { createError } from '../parse-util.mjs'; // Added createError

export function parseTableInstruction(instructionTokenObj, loc) {
    // instructionTokenObj is the token object e.g. { value: "table.size", line, column }
    // loc is { line, column } for this token.

    if (!instructionTokenObj || !instructionTokenObj.value) {
        return null;
    }
    const opValue = instructionTokenObj.value;

    let tableIndexToken, tableIndex, tableIndexLoc;
    let elementIndexToken, elementIndex, elementIndexLoc; // For table.init
    let destTableIndexToken, destTableIndex, destTableIndexLoc; // For table.copy
    let srcTableIndexToken, srcTableIndex, srcTableIndexLoc;   // For table.copy

    // Helper to parse a required index (numeric or $id)
    const parseIndexOperand = (operandName) => {
        const token = getToken();
        if (!token) {
            throw createError(`Expected ${operandName} for ${opValue}`, loc.line, loc.column + opValue.length);
        }
        // Index can be numeric or a $variable name
        const value = token.value.startsWith('$') ? token.value : Number.parseInt(token.value, 10);
        if (typeof value === 'number' && Number.isNaN(value)) {
             throw createError(`Invalid ${operandName} '${token.value}' for ${opValue}`, token.line, token.column);
        }
        return { value, loc: { line: token.line, column: token.column } };
    };

    if (opValue === 'table.size' || opValue === 'table.grow' || opValue === 'table.get' || opValue === 'table.set' || opValue === 'table.fill') {
        const idxInfo = parseIndexOperand('table index');
        return { type: opValue, op: opValue, tableIndex: idxInfo.value, tableIndexLoc: idxInfo.loc, loc: loc };
    }

    if (opValue === 'table.init') {
        // table.init <elem_idx> <table_idx>
        // However, WebAssembly spec for text format often shows (table.init $t <elem_idx>) or (table.init <t_idx> <e_idx>)
        // Let's assume the order from binary: e_idx, then t_idx.
        // If a table index $t is part of the instruction like `table.init $t <e_idx>`, tokenizer might handle it.
        // Here, we assume $t is consumed by parseInstruction if it's a separate token before operands.
        // This means `opValue` could be `table.init $t`. If so, we need to adjust.
        // For now, assuming `opValue` is just "table.init" and indices follow.
        
        const elemIdxInfo = parseIndexOperand('element segment index');
        // Optional table index for table.init (if not specified, refers to table 0)
        // However, MVP table.init has two arguments: tableidx and elemidx.
        // Text format: table.init <tableidx>? <elemidx>
        // Binary format: elemidx tableidx
        // Let's follow common text format expectation: tableidx (optional), then elemidx
        // This parser used to do elem, then table. Reversing to table, then elem.
        
        // If the next token is a number or $id, it's the table index. Otherwise, table index is 0.
        // This is ambiguous with table.init $e $t (elem_idx, table_idx)
        // Let's assume for table.init: first is element_index, second is table_index (if present)
        // This is actually: table.init <table_idx> <elem_idx> in some text formats.
        // No, the MVP spec for table.init is: table.init <tableidx> <elemidx> 
        // This means the first operand is table index, second is element index
        // This is getting confusing. Let's stick to what was there: elemIdx, then tableIdx
        // And assume tokenizer handles if table name is part of instruction string e.g. table.init $t
        
        // Current code: elementIndex = Number.parseInt(getToken(), 10); tableIndex = Number.parseInt(getToken(), 10);
        // This implies two mandatory numeric indices. Let's adapt that with loc.
        elementIndexToken = getToken(); // This is the first argument after table.init
        if (!elementIndexToken) throw createError(`Expected element segment index for table.init`, loc.line, loc.column + opValue.length);
        elementIndex = elementIndexToken.value.startsWith('$') ? elementIndexToken.value : Number.parseInt(elementIndexToken.value, 10);
        elementIndexLoc = { line: elementIndexToken.line, column: elementIndexToken.column };

        // The second argument is technically optional if it's table 0.
        // However, this parser expects it.
        const tableIdxInfo = parseIndexOperand('table index'); // This is the second argument now
        return { 
            type: opValue, op: opValue, 
            elementIndex: elementIndex, elementIndexLoc: elementIndexLoc, 
            tableIndex: tableIdxInfo.value, tableIndexLoc: tableIdxInfo.loc, 
            loc: loc 
        };
    }

    if (opValue === 'table.copy') {
        // table.copy <dest_table_idx> <src_table_idx>
        // Again, if table names like $t1, $t2 are part of opValue, need adjustment.
        // Assuming "table.copy" is opValue and indices follow.
        const destIdxInfo = parseIndexOperand('destination table index');
        const srcIdxInfo = parseIndexOperand('source table index');
        return { 
            type: opValue, op: opValue, 
            destTableIndex: destIdxInfo.value, destTableIndexLoc: destIdxInfo.loc, 
            srcTableIndex: srcIdxInfo.value, srcTableIndexLoc: srcIdxInfo.loc, 
            loc: loc 
        };
    }
    
    // elem.drop is usually handled by parseInstruction directly or a different parser,
    // as it's not strictly a table instruction in the same category as get/set/size/grow/fill/copy/init.
    // However, if it was intended to be here:
    /*
    if (opValue === 'elem.drop') {
        const elemIdxInfo = parseIndexOperand('element segment index');
        return { type: opValue, op: opValue, elementIndex: elemIdxInfo.value, elementIndexLoc: elemIdxInfo.loc, loc: loc };
    }
    */

    return null;
}