// Generated by 🤖
import { atEnd, getToken, peekToken } from '../tape.mjs';

export function parseMemoryOpsInstruction(instructionTokenObj, loc) {
    // instructionTokenObj is the token object e.g. { value: "i32.load offset=4", line, column }
    // loc is { line, column } for this token.
    
    // atEnd() might not be strictly necessary if parseInstruction checks before calling,
    // but good for safety if this can be called in other contexts.
    if (atEnd() && !instructionTokenObj) return null; // Guard against empty tape if called unexpectedly
    if (!instructionTokenObj || !instructionTokenObj.value) return null;

    const opFullValue = instructionTokenObj.value; // e.g. "i32.load8_s offset=4 align=1"
    
    // Helper to parse memory arguments (offset, align) from the full operation string
    // and to determine the base operation type.
    const parseMemArgs = (fullOpStr, defaultAlignVal) => {
        let align = defaultAlignVal;
        let offset = 0;
        const offsetMatch = fullOpStr.match(/offset=(\d+)/);
        if (offsetMatch && offsetMatch[1]) {
            offset = Number.parseInt(offsetMatch[1], 10);
        }
        const alignMatch = fullOpStr.match(/align=(\d+)/); // Align must be non-negative
        if (alignMatch && alignMatch[1]) {
            align = Number.parseInt(alignMatch[1], 10);
            // TODO: Validate align against natural alignment of op
        }
        const baseOp = fullOpStr.split(/\s+/)[0]; // e.g., "i32.load8_s"
        return { baseOp, memarg: { offset, align } };
    };

    let parsedMemArgs;
    let defaultAlign;
    let memoryRef = null, memoryRefLoc = null; // For optional memory index like $mem or 0

    // i32 load operations
    if (opFullValue.startsWith('i32.load')) {
        if (opFullValue.includes('load8_s') || opFullValue.includes('load8_u')) defaultAlign = 0;
        else if (opFullValue.includes('load16_s') || opFullValue.includes('load16_u')) defaultAlign = 1;
        else defaultAlign = 2; // i32.load
        parsedMemArgs = parseMemArgs(opFullValue, defaultAlign);
    }
    // i64 load operations
    else if (opFullValue.startsWith('i64.load')) {
        if (opFullValue.includes('load8_s') || opFullValue.includes('load8_u')) defaultAlign = 0;
        else if (opFullValue.includes('load16_s') || opFullValue.includes('load16_u')) defaultAlign = 1;
        else if (opFullValue.includes('load32_s') || opFullValue.includes('load32_u')) defaultAlign = 2;
        else defaultAlign = 3; // i64.load
        parsedMemArgs = parseMemArgs(opFullValue, defaultAlign);
    }
    // i32 store operations
    else if (opFullValue.startsWith('i32.store')) {
        if (opFullValue.includes('store8')) defaultAlign = 0;
        else if (opFullValue.includes('store16')) defaultAlign = 1;
        else defaultAlign = 2; // i32.store
        parsedMemArgs = parseMemArgs(opFullValue, defaultAlign);
    }
    // i64 store operations
    else if (opFullValue.startsWith('i64.store')) {
        if (opFullValue.includes('store8')) defaultAlign = 0;
        else if (opFullValue.includes('store16')) defaultAlign = 1;
        else if (opFullValue.includes('store32')) defaultAlign = 2;
        else defaultAlign = 3; // i64.store
        parsedMemArgs = parseMemArgs(opFullValue, defaultAlign);
    }
    // f32 load/store
    else if (opFullValue.startsWith('f32.load')) {
        parsedMemArgs = parseMemArgs(opFullValue, 2); // 4 bytes alignment
    } else if (opFullValue.startsWith('f32.store')) {
        parsedMemArgs = parseMemArgs(opFullValue, 2);
    }
    // f64 load/store
    else if (opFullValue.startsWith('f64.load')) {
        parsedMemArgs = parseMemArgs(opFullValue, 3); // 8 bytes alignment
    } else if (opFullValue.startsWith('f64.store')) {
        parsedMemArgs = parseMemArgs(opFullValue, 3);
    } else {
        return null; // Not a recognized memory operation for this parser
    }

    // Check for explicit memory reference (index or name) if multi-memory is enabled.
    // This comes *after* the main instruction token (which might include offset/align).
    // Example: i32.load offset=4 $mem_name
    // Example: i32.load offset=4 0
    const nextToken = peekToken(); // Token after the main instruction token
    if (nextToken && (nextToken.value.startsWith('$') || /^\d+$/.test(nextToken.value))) {
        // Check if it's not an offset or align attribute for another instruction.
        // This logic is tricky if the tokenizer doesn't separate these clearly.
        // Assuming if it's $name or number here, it's a memory index.
        // More robust parsing would require the main `parseInstruction` to handle this.
        // For now, if `parseInstruction` only passes the first token, this won't be hit
        // unless the memory index is *also* part of the combined token string (unlikely).
        // This part of the code might be unreachable if parseInstruction only sends the first token.
        // If `parseInstruction` handles consuming memory index itself, then this is not needed here.
        // Let's assume for now that `parseInstruction` would have consumed the memory index if it were separate.
        // So, `memoryRef` remains null unless the tokenizer itself includes it in `opFullValue` (which is not standard).
    }
    
    // The 'type' and 'op' fields are the base operation type (e.g. "i32.load8_s")
    return { 
        type: parsedMemArgs.baseOp, 
        op: parsedMemArgs.baseOp, // Often 'op' is used for the specific opcode string
        memarg: parsedMemArgs.memarg, // { offset, align }
        // memoryRef and memoryRefLoc would be set here if parsed
        loc: loc 
    };
}