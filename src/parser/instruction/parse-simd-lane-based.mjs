// Generated by 🤖
import { atEnd, peekToken, skipToken, getToken } from '../tape.mjs';
import { createError, parseDecimalOrHex, parseSigned64BitHex } from '../parse-util.mjs';

export function parseSIMDLaneBased(instructionTokenObj, loc) {
    // instructionTokenObj is the token object e.g. { value: "v128.const", line, column }
    // loc is { line, column } for this token.

    if (!instructionTokenObj || !instructionTokenObj.value) {
        return null; 
    }
    const opValue = instructionTokenObj.value;


    // Handle v128.const instruction
    if (opValue === 'v128.const') {
        const shapeToken = getToken();
        if (!shapeToken) throw createError("Expected shape for v128.const", loc.line, loc.column + opValue.length);
        const shape = shapeToken.value;
        const shapeLoc = { line: shapeToken.line, column: shapeToken.column };
        
        const immediates = []; // Array of { value: number | BigInt, loc: {line, column} }

        let maxValues;
        if (shape === 'i8x16' || shape === 'i8x16_splat') maxValues = 16;
        else if (shape === 'i16x8' || shape === 'i16x8_splat') maxValues = 8;
        else if (shape === 'i32x4' || shape === 'i32x4_splat' || shape === 'f32x4' || shape === 'f32x4_splat') maxValues = 4;
        else if (shape === 'i64x2' || shape === 'i64x2_splat' || shape === 'f64x2' || shape === 'f64x2_splat') maxValues = 2;
        else throw createError(`Invalid shape '${shape}' for v128.const`, shapeLoc.line, shapeLoc.column);

        let nextImmediateToken = peekToken();
        while (nextImmediateToken && nextImmediateToken.value !== ')' && immediates.length < maxValues) {
            // Regex for numbers, including hex and potentially floats
            if (/^-?(0x[0-9a-fA-F]+|\d+(\.\d+)?(e[+-]?\d+)?)$/i.test(nextImmediateToken.value)) {
                const immToken = getToken();
                const isFloat = shape.startsWith('f') || immToken.value.includes('.') || immToken.value.toLowerCase().includes('e');
                const is64Bit = shape.startsWith('i64') || shape.startsWith('f64');
                let value;
                if (isFloat) {
                    value = Number.parseFloat(immToken.value);
                } else if (is64Bit) {
                    value = parseSigned64BitHex(immToken.value); // Assumes hex or will be parsed as decimal BigInt
                } else {
                    value = parseDecimalOrHex(immToken.value);
                }
                immediates.push({ value, loc: { line: immToken.line, column: immToken.column } });
            } else {
                throw createError(`Expected numeric immediate for v128.const ${shape}, got '${nextImmediateToken.value}'`, nextImmediateToken.line, nextImmediateToken.column);
            }
            nextImmediateToken = peekToken();
        }

        if (immediates.length !== maxValues) {
            const lastLoc = immediates.length > 0 ? immediates[immediates.length-1].loc : shapeLoc;
            throw createError(`v128.const with shape ${shape} requires exactly ${maxValues} values, got ${immediates.length}`, lastLoc.line, lastLoc.column);
        }
        return { type: 'v128.const', shape, shapeLoc, immediates, loc };
    }

    // Handle i8x16.shuffle instruction
    if (opValue === 'i8x16.shuffle') {
        const laneIndices = []; // Array of { value: number, loc: {line, column} }
        let nextLaneToken = peekToken();
        while (nextLaneToken && nextLaneToken.value !== ')' && laneIndices.length < 16) {
            if (/^[0-9]+$/.test(nextLaneToken.value)) {
                const indexToken = getToken();
                const index = Number.parseInt(indexToken.value, 10);
                if (index < 0 || index > 31) { // For i8x16.shuffle, indices can be 0-31 (referencing two 16-byte vectors)
                    throw createError(`i8x16.shuffle lane index must be 0-31, got ${index}`, indexToken.line, indexToken.column);
                }
                laneIndices.push({ value: index, loc: { line: indexToken.line, column: indexToken.column } });
            } else {
                 throw createError(`Expected numeric lane index for i8x16.shuffle, got '${nextLaneToken.value}'`, nextLaneToken.line, nextLaneToken.column);
            }
            nextLaneToken = peekToken();
        }

        if (laneIndices.length !== 16) {
            const lastLoc = laneIndices.length > 0 ? laneIndices[laneIndices.length-1].loc : loc;
            throw createError(`i8x16.shuffle requires exactly 16 lane indices, got ${laneIndices.length}`, lastLoc.line, lastLoc.column);
        }
        return { type: 'i8x16.shuffle', laneIndices, loc };
    }

    // Handle extract_lane instructions
    const extractLaneOpcodes = [
        'i8x16.extract_lane_s', 'i8x16.extract_lane_u',
        'i16x8.extract_lane_s', 'i16x8.extract_lane_u',
        'i32x4.extract_lane', 'i64x2.extract_lane',
        'f32x4.extract_lane', 'f64x2.extract_lane'
    ];
    if (extractLaneOpcodes.includes(opValue)) {
        const laneIndexToken = getToken();
        if (!laneIndexToken || !/^\d+$/.test(laneIndexToken.value)) {
            throw createError(`Expected lane index for ${opValue}`, loc.line, loc.column + opValue.length);
        }
        const laneIndex = Number.parseInt(laneIndexToken.value, 10);
        const laneIndexLoc = { line: laneIndexToken.line, column: laneIndexToken.column };

        // Validate lane index
        let maxLaneIdx = 0;
        if (opValue.startsWith('i8x16')) maxLaneIdx = 15;
        else if (opValue.startsWith('i16x8')) maxLaneIdx = 7;
        else if (opValue.startsWith('i32x4') || opValue.startsWith('f32x4')) maxLaneIdx = 3;
        else if (opValue.startsWith('i64x2') || opValue.startsWith('f64x2')) maxLaneIdx = 1;
        
        if (laneIndex < 0 || laneIndex > maxLaneIdx) {
            throw createError(`${opValue} lane index must be 0-${maxLaneIdx}, got ${laneIndex}`, laneIndexLoc.line, laneIndexLoc.column);
        }
        return { type: opValue, laneIndex, laneIndexLoc, loc };
    }

    // Handle replace_lane instructions
    const replaceLaneOpcodes = [
        'i8x16.replace_lane', 'i16x8.replace_lane',
        'i32x4.replace_lane', 'i64x2.replace_lane',
        'f32x4.replace_lane', 'f64x2.replace_lane'
    ];
    if (replaceLaneOpcodes.includes(opValue)) {
        const laneIndexToken = getToken();
         if (!laneIndexToken || !/^\d+$/.test(laneIndexToken.value)) {
            throw createError(`Expected lane index for ${opValue}`, loc.line, loc.column + opValue.length);
        }
        const laneIndex = Number.parseInt(laneIndexToken.value, 10);
        const laneIndexLoc = { line: laneIndexToken.line, column: laneIndexToken.column };

        let maxLaneIdx = 0;
        if (opValue.startsWith('i8x16')) maxLaneIdx = 15;
        else if (opValue.startsWith('i16x8')) maxLaneIdx = 7;
        else if (opValue.startsWith('i32x4') || opValue.startsWith('f32x4')) maxLaneIdx = 3;
        else if (opValue.startsWith('i64x2') || opValue.startsWith('f64x2')) maxLaneIdx = 1;

        if (laneIndex < 0 || laneIndex > maxLaneIdx) {
            throw createError(`${opValue} lane index must be 0-${maxLaneIdx}, got ${laneIndex}`, laneIndexLoc.line, laneIndexLoc.column);
        }
        return { type: opValue, laneIndex, laneIndexLoc, loc };
    }

    return null; // Changed from false to null for consistency
}