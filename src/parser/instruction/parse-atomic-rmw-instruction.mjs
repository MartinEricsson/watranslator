// Generated by 🤖
import { atEnd } from '../tape.mjs'; // atEnd might not be needed if parseInstruction checks first

export function parseAtomicRMWInstruction(instructionTokenObj, loc) {
    // instructionTokenObj is the token object e.g. { value: "i32.atomic.rmw.add offset=4", line, column }
    // loc is { line, column } for this token.

    if (!instructionTokenObj || !instructionTokenObj.value) {
        return null; 
    }
    const opValue = instructionTokenObj.value;

    if (opValue.startsWith('i32.atomic.rmw')) {
        let align = 2; // Default for i32 (4 bytes = 2^2)
        if (opValue.includes('rmw8')) align = 0; // 8-bit ops (1 byte = 2^0)
        else if (opValue.includes('rmw16')) align = 1; // 16-bit ops (2 bytes = 2^1)

        let offset = 0;
        const offsetMatch = opValue.match(/offset=(\d+)/);
        if (offsetMatch && offsetMatch[1]) {
            offset = Number.parseInt(offsetMatch[1], 10);
        }

        const alignMatch = opValue.match(/align=(\d+)/); // Align must be non-negative for atomics
        if (alignMatch && alignMatch[1]) {
            // Override default align if explicitly provided and valid for the operation width
            const explicitAlign = Number.parseInt(alignMatch[1], 10);
            // Basic validation: e.g. for rmw8, align must be 0. for rmw16, align <=1. for rmw, align <=2.
            // This simple check doesn't prevent invalid combinations like i32.atomic.rmw16 align=2
            // More robust validation should happen during a validation phase or require tokenizer to separate these.
            align = explicitAlign; 
        }
        
        // The 'op' field usually refers to the core operation, not including memargs string.
        // However, if tokenizer provides "i32.atomic.rmw.add offset=4", then opValue is that whole string.
        // For now, type and op will be the full string from tokenizer.
        return { type: opValue, op: opValue, memarg: { align, offset }, loc: loc };
    }

    if (opValue.startsWith('i64.atomic.rmw')) {
        let align = 3; // Default for i64 (8 bytes = 2^3)
        if (opValue.includes('rmw8')) align = 0;
        else if (opValue.includes('rmw16')) align = 1;
        else if (opValue.includes('rmw32')) align = 2;

        let offset = 0;
        const offsetMatch = opValue.match(/offset=(\d+)/);
        if (offsetMatch && offsetMatch[1]) {
            offset = Number.parseInt(offsetMatch[1], 10);
        }

        const alignMatch = opValue.match(/align=(\d+)/);
        if (alignMatch && alignMatch[1]) {
            align = Number.parseInt(alignMatch[1], 10);
        }

        return { type: opValue, op: opValue, memarg: { align, offset }, loc: loc };
    }

    return null;
}