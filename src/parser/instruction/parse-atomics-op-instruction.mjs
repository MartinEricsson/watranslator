// Generated by 🤖
import { atEnd } from '../tape.mjs'; // atEnd might not be needed if parseInstruction checks first

export function parseAtomicsOpInstruction(instructionTokenObj, loc) {
    // instructionTokenObj is the token object e.g. { value: "i32.atomic.load offset=4", line, column }
    // loc is { line, column } for this token.

    if (!instructionTokenObj || !instructionTokenObj.value) {
        return null;
    }
    const opFullValue = instructionTokenObj.value; // e.g., "i32.atomic.load8_u offset=4 align=1"

    if (opFullValue.startsWith('i32.atomic.') || opFullValue.startsWith('i64.atomic.')) {
        const is64Bit = opFullValue.startsWith('i64.');
        const isStore = opFullValue.includes('.store');
        
        // Determine default alignment based on operation width
        // This logic assumes the specific part like "load8_u", "store16" etc. dictates the size.
        let defaultAlign;
        if (opFullValue.includes('8_') || opFullValue.includes('store8') || opFullValue.includes('rmw8')) { // Added rmw8 for completeness
            defaultAlign = 0; // 1 byte
        } else if (opFullValue.includes('16_') || opFullValue.includes('store16') || opFullValue.includes('rmw16')) {
            defaultAlign = 1; // 2 bytes
        } else if (opFullValue.includes('32_') || opFullValue.includes('store32') || opFullValue.includes('rmw32')) {
            defaultAlign = 2; // 4 bytes
        } else {
            // Full-sized operations (32-bit for i32, 64-bit for i64)
            defaultAlign = is64Bit ? 3 : 2; // 8 bytes or 4 bytes
        }

        let align = defaultAlign; // Start with default, may be overridden by explicit align attribute

        // Extract offset if present
        let offset = 0;
        const offsetMatch = opFullValue.match(/offset=(\d+)/);
        if (offsetMatch && offsetMatch[1]) {
            offset = Number.parseInt(offsetMatch[1], 10);
        }

        // Extract align if present and override default
        const alignMatch = opFullValue.match(/align=(\d+)/); // Align must be non-negative
        if (alignMatch && alignMatch[1]) {
            align = Number.parseInt(alignMatch[1], 10);
            // TODO: Validate that explicit align is not greater than natural alignment if strictness is needed.
            // E.g., i32.atomic.load8_u align=2 is invalid. Max align for 8_u is 0.
        }

        // Get the base instruction type (e.g., "i32.atomic.load", "i64.atomic.store8_u")
        // This strips off any " offset=X align=Y" part for the 'type' and 'op' fields.
        const opBase = opFullValue.split(/\s+/)[0];

        return { type: opBase, op: opBase, memarg: { offset, align }, loc: loc };
    }

    return null;
}