// Generated by 🤖
import { atEnd, peekToken, skipToken, getToken } from '../tape.mjs';
import { createError } from '../parse-util.mjs';

// Helper to parse optional offset=N and align=N attributes that might follow a memory instruction.
// These attributes are separate tokens.
function parseOptionalMemoryAttributes() {
    let align = null, alignLoc = null; // Null means instruction-defined default, 0 is a valid explicit align
    let offset = 0, offsetLoc = null;  // Default offset is 0 if not specified

    // Process attributes in any order
    // This loop assumes that offset/align attributes directly follow the main instruction,
    // before any other operands like lane_idx for lane operations.
    while (true) {
        const attrTokenObj = peekToken();
        if (!attrTokenObj) break;

        const attrValue = attrTokenObj.value;
        if (attrValue.startsWith('offset=')) {
            skipToken(); // Consume the offset token
            const valStr = attrValue.substring('offset='.length);
            if (!/^\d+$/.test(valStr)) throw createError(`Invalid offset value: ${valStr}`, attrTokenObj.line, attrTokenObj.column);
            offset = Number.parseInt(valStr, 10);
            offsetLoc = { line: attrTokenObj.line, column: attrTokenObj.column };
        } else if (attrValue.startsWith('align=')) {
            skipToken(); // Consume the align token
            const valStr = attrValue.substring('align='.length);
            if (!/^\d+$/.test(valStr)) throw createError(`Invalid align value: ${valStr}`, attrTokenObj.line, attrTokenObj.column);
            align = Number.parseInt(valStr, 10);
            alignLoc = { line: attrTokenObj.line, column: attrTokenObj.column };
        } else {
            // Not a memory attribute we recognize as a separate token immediately following.
            break;
        }
    }
    return { offset, offsetLoc, align, alignLoc };
}

export function parseSIMDMemory(instructionTokenObj, loc) {
    // instructionTokenObj is the token object e.g. { value: "v128.load", line, column }
    // loc is { line, column } for this token.
    
    if (!instructionTokenObj || !instructionTokenObj.value) {
        return null; 
    }
    const opValue = instructionTokenObj.value; // e.g. "v128.load", "v128.load8_lane"

    const basicOps = [
        'v128.load', 'v128.store',
        'v128.load8x8_s', 'v128.load8x8_u',
        'v128.load16x4_s', 'v128.load16x4_u',
        'v128.load32x2_s', 'v128.load32x2_u',
        'v128.load8_splat', 'v128.load16_splat',
        'v128.load32_splat', 'v128.load64_splat',
        'v128.load32_zero', 'v128.load64_zero'
    ];

    const laneAccessOps = [
        'v128.load8_lane', 'v128.load16_lane',
        'v128.load32_lane', 'v128.load64_lane',
        'v128.store8_lane', 'v128.store16_lane',
        'v128.store32_lane', 'v128.store64_lane'
    ];

    if (basicOps.includes(opValue)) {
        const { offset, offsetLoc, align, alignLoc } = parseOptionalMemoryAttributes();
        // Default align calculation based on opValue (if align is still null)
        let finalAlign = align;
        if (finalAlign === null) { // if not explicitly set by align=N
            if (opValue.includes('8x8') || opValue.includes('load8_splat')) finalAlign = 0; // 1 byte
            else if (opValue.includes('16x4') || opValue.includes('load16_splat')) finalAlign = 1; // 2 bytes
            else if (opValue.includes('32x2') || opValue.includes('load32_splat') || opValue.includes('load32_zero')) finalAlign = 2; // 4 bytes
            else if (opValue.includes('load64_splat') || opValue.includes('load64_zero')) finalAlign = 3; // 8 bytes
            else if (opValue === 'v128.load' || opValue === 'v128.store') finalAlign = 4; // 16 bytes (2^4)
            else finalAlign = 0; // Fallback, should be specific
        }
        return { type: opValue, memarg: { offset, align: finalAlign }, offsetLoc, alignLoc, loc };
    }

    if (laneAccessOps.includes(opValue)) {
        const { offset, offsetLoc, align, alignLoc } = parseOptionalMemoryAttributes();
        
        // Parse lane index (must follow offset/align attributes)
        const laneIndexToken = getToken();
        if (!laneIndexToken || !/^\d+$/.test(laneIndexToken.value)) {
            throw createError(`Expected lane index for ${opValue}`, 
                              (alignLoc || offsetLoc || loc).line, 
                              (alignLoc || offsetLoc || loc).column + (align !== null ? 5 : (offset !== 0 ? 5 : opValue.length)));
        }
        const laneIndex = Number.parseInt(laneIndexToken.value, 10);
        const laneIndexLoc = { line: laneIndexToken.line, column: laneIndexToken.column };

        // Validate lane index & determine default align if not specified by align=N
        let maxLaneIdx = 0;
        let defaultAlignForOp = 0;
        if (opValue.includes('8_lane')) { maxLaneIdx = 15; defaultAlignForOp = 0; }
        else if (opValue.includes('16_lane')) { maxLaneIdx = 7; defaultAlignForOp = 1; }
        else if (opValue.includes('32_lane')) { maxLaneIdx = 3; defaultAlignForOp = 2; }
        else if (opValue.includes('64_lane')) { maxLaneIdx = 1; defaultAlignForOp = 3; }

        if (laneIndex < 0 || laneIndex > maxLaneIdx) {
            throw createError(`${opValue} has invalid lane index: ${laneIndex}. Must be 0-${maxLaneIdx}.`, laneIndexLoc.line, laneIndexLoc.column);
        }
        
        const finalAlign = align === null ? defaultAlignForOp : align;

        return { type: opValue, memarg: { offset, align: finalAlign }, offsetLoc, alignLoc, laneIndex, laneIndexLoc, loc };
    }

    return null; // Not a SIMD memory op handled by this parser.
}