// Generated by 🤖
import { atEnd, getToken, peekToken } from '../tape.mjs'; // Added peekToken for safety, though getToken might be enough if next token is guaranteed
import { createError } from '../parse-util.mjs';


export function parseRefInstruction(instructionTokenObj, loc) {
    // instructionTokenObj is the token object e.g. { value: "ref.func", line, column }
    // loc is { line, column } for this token.

    if (!instructionTokenObj || !instructionTokenObj.value) {
        return null; 
    }
    const opValue = instructionTokenObj.value;

    if (opValue === 'ref.func') {
        const funcToken = getToken();
        if (!funcToken) {
            throw createError("Expected function index or name after ref.func", loc.line, loc.column + opValue.length);
        }
        // funcToken.value could be a numeric index or a $name
        return { 
            type: 'ref.func', 
            funcIdentifier: funcToken.value, 
            funcIdentifierLoc: { line: funcToken.line, column: funcToken.column }, 
            loc: loc 
        };
    }

    if (opValue === 'ref.null') {
        const refTypeToken = getToken();
        if (!refTypeToken) {
            throw createError("Expected reference type (e.g., func, extern) after ref.null", loc.line, loc.column + opValue.length);
        }
        // refTypeToken.value should be 'func' or 'extern' or other valid ref types
        const validRefTypes = ['func', 'extern', 'any']; // anyref, funcref, externref are common. 'any' is more abstract.
        // For simplicity, we'll accept what's given, validation might be separate.
        return { 
            type: 'ref.null', 
            heapType: refTypeToken.value, 
            heapTypeLoc: { line: refTypeToken.line, column: refTypeToken.column }, 
            loc: loc 
        };
    }

    return null;
}