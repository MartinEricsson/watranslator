// Generated by 🤖
import { createError } from './error.mjs';
import { compileVariableAccess } from './variable-access.mjs';
import { compileVariableConstants } from './variable-constants.mjs';
import { compileStackInstruction } from './stack-instruction.mjs';
import { compileTableInstruction } from './table.mjs';
import { compileMemoryInstruction } from './memory.mjs';
import { compileMemoryAccess } from './memory-access.mjs';
import { compileFunctionCall } from './function-call.mjs';
import { compileControlFlow } from './control-flow.mjs';
import { compileReferenceOpcodes } from './reference-operation.mjs';
import { compileAtomicLoadStore } from './atomic-load-store.mjs';
import { compileAtomicWaitNotify } from './atomic-wait-notify.mjs';
import { compileAtomicOps } from './atomic-ops.mjs';
import { compileNonTrappingFloatToInt } from './non-trapping-float-to-int.mjs';
import { compileSIMDLaneBased } from './simd-lane-based.mjs';
import { compileSIMDMemory } from './simd-memory.mjs';
import { compileSIMDPlainOpcode } from './simd-plain-opcode.mjs';
// Note: addMapping import is not needed here if all sub-compilers handle it.

export function compileInstruction(instr, func, binary, moduleFunctions, moduleGlobals, module) {
    // Each sub-compiler is responsible for:
    // 1. Checking if instr.loc exists.
    // 2. Calling addMapping(instr.loc.line, instr.loc.column, binary.length) BEFORE adding bytes.
    // 3. Appending its bytes to `binary`.
    // 4. Returning true if handled.

    if (compileVariableAccess(instr, func, binary, moduleGlobals, module)) {
        return;
    }

    if (compileVariableConstants(instr, binary)) { // Pass `instr` for loc
        return;
    }

    if (compileControlFlow(instr, func, binary, moduleFunctions, moduleGlobals, module)) {
        return;
    }

    if (compileStackInstruction(instr, binary)) { // Pass `instr` for loc
        return;
    }

    if (compileMemoryAccess(instr, func, binary, module)) { // Pass `instr` for loc
        return;
    }

    // compileMemoryInstruction seems unused, compileMemoryAccess is likely for load/store ops
    // if (compileMemoryInstruction(instr, binary, module)) { 
    //     return;
    // }

    if (compileFunctionCall(instr, binary, module, func, moduleFunctions)) { // Pass `instr` for loc
        return;
    }

    if (compileTableInstruction(instr, func, module, binary)) { // Pass `instr` for loc
        return;
    }

    if (compileReferenceOpcodes(instr, func, module, binary)) { // Pass `instr` for loc
        return;
    }

    if (compileAtomicLoadStore(instr, func, binary, module)) {
        return;
    }

    if (compileAtomicWaitNotify(instr, func, binary, module)) {
        return;
    }

    if (compileAtomicOps(instr, func, binary, module)) {
        return;
    }

    if (compileSIMDLaneBased(instr, binary)) { // Pass `instr` for loc
        return;
    }

    if (compileNonTrappingFloatToInt(instr, binary)) { // Pass `instr` for loc
        return;
    }

    if (compileSIMDMemory(instr, binary)) { // Pass `instr` for loc
        return;
    }

    if (compileSIMDPlainOpcode(instr, binary)) { // Pass `instr` for loc
        return;
    }

    // If no compiler handled it, throw an error.
    // The error should ideally use instr.loc if available.
    const errorBase = { func, module };
    if (instr && instr.loc) {
        errorBase.line = instr.loc.line;
        errorBase.column = instr.loc.column;
    }
    throw createError(instr, func, module, `Unsupported instruction type: ${instr.type || instr.op}. Check for typos or refer to the WebAssembly specification.`);
}