// Generated by 🤖
import { encodeULEB128 } from "../compile-utils.mjs";
import { addMapping } from "../../sourcemap.mjs";
// Assuming wasmConstants.INSTR might define these, or using literals directly.
// For this example, direct literals are used as per original file, but ideally from wasmConstants.
const ATOMIC_PREFIX = 0xFE;
const ATOMIC_I32_LOAD = 0x10;
const ATOMIC_I64_LOAD = 0x11;
const ATOMIC_I32_LOAD8_U = 0x12;
const ATOMIC_I32_LOAD16_U = 0x13;
const ATOMIC_I64_LOAD8_U = 0x14;
const ATOMIC_I64_LOAD16_U = 0x15;
const ATOMIC_I64_LOAD32_U = 0x16;
const ATOMIC_I32_STORE = 0x17;
const ATOMIC_I64_STORE = 0x18;
const ATOMIC_I32_STORE8 = 0x19;
const ATOMIC_I32_STORE16 = 0x1A;
const ATOMIC_I64_STORE8 = 0x1B;
const ATOMIC_I64_STORE16 = 0x1C;
const ATOMIC_I64_STORE32 = 0x1D;

const ATOMIC_INSTR_MAP = new Map([
    ['i32.atomic.load', ATOMIC_I32_LOAD], ['i64.atomic.load', ATOMIC_I64_LOAD],
    ['i32.atomic.load8_u', ATOMIC_I32_LOAD8_U], ['i32.atomic.load16_u', ATOMIC_I32_LOAD16_U],
    ['i64.atomic.load8_u', ATOMIC_I64_LOAD8_U], ['i64.atomic.load16_u', ATOMIC_I64_LOAD16_U],
    ['i64.atomic.load32_u', ATOMIC_I64_LOAD32_U],
    ['i32.atomic.store', ATOMIC_I32_STORE], ['i32.atomic.store8', ATOMIC_I32_STORE8],
    ['i32.atomic.store16', ATOMIC_I32_STORE16],
    ['i64.atomic.store', ATOMIC_I64_STORE], ['i64.atomic.store8', ATOMIC_I64_STORE8],
    ['i64.atomic.store16', ATOMIC_I64_STORE16], ['i64.atomic.store32', ATOMIC_I64_STORE32],
]);

export function compileAtomicLoadStore(instr, func, binary, module) { // Changed body to binary
    const instrType = instr.op || instr.type;

    if (!ATOMIC_INSTR_MAP.has(instrType)) {
        return false;
    }

    if (instr.loc && instr.loc.line != null) {
        addMapping(instr.loc.line, instr.loc.column, binary.length);
    }

    binary.push(ATOMIC_PREFIX);
    binary.push(ATOMIC_INSTR_MAP.get(instrType));

    let defaultAlign = 2; // For i32
    if (instrType.startsWith('i64.')) defaultAlign = 3; // For i64

    if (instrType.includes('8_') || instrType.includes('store8')) defaultAlign = 0;
    else if (instrType.includes('16_') || instrType.includes('store16')) defaultAlign = 1;
    else if (instrType.includes('32_') || instrType.includes('store32')) defaultAlign = 2;
    // Note: For full i64 (e.g. i64.atomic.load), defaultAlign is already 3.
    // For full i32 (e.g. i32.atomic.load), defaultAlign is already 2.

    const alignment = instr.memarg && instr.memarg.align != null ? instr.memarg.align : defaultAlign;
    // The parser for atomic ops (like parseAtomicsOpInstruction) stores memarg: {offset, align}
    // So, instr.align and instr.offset might not be direct properties.
    const offset = instr.memarg && instr.memarg.offset != null ? instr.memarg.offset : 0;
    
    // TODO: Add validation for alignment against natural alignment if necessary

    binary.push(...encodeULEB128(alignment));
    binary.push(...encodeULEB128(offset));

    return true;
}