// Generated by 🤖
import wasmConstants from "../constants.mjs";
import { encodeSLEB128 } from "../compile-utils.mjs";
import { addMapping } from "../../sourcemap.mjs";
import { createError } from "./error.mjs";


const { TYPE, INSTR } = wasmConstants; // Added INSTR if opcodes are defined there

// Assuming opcodes are defined in wasmConstants.INSTR for consistency
const REF_NULL_OPCODE = INSTR.REF_NULL || 0xD0; 
const REF_FUNC_OPCODE = INSTR.REF_FUNC || 0xD2; 
const REF_IS_NULL_OPCODE = INSTR.REF_IS_NULL || 0xD1;

export function compileReferenceOpcodes(instr, func, module, binary) { // Changed body to binary
    if (instr.type === 'ref.null') {
        if (instr.loc && instr.loc.line != null) {
            addMapping(instr.loc.line, instr.loc.column, binary.length);
        }
        binary.push(REF_NULL_OPCODE);
        
        // instr.refType or instr.heapType from parser
        const heapType = instr.heapType || instr.refType; 
        if (!heapType) {
            throw createError(instr, func, module, "ref.null requires a heap type (e.g., func, extern).");
        }
        // Push the reference type (func or extern)
        // Standard heap types are funcref (0x70), externref (0x6F).
        // Parser should provide these specific values or translatable strings.
        let typeByte;
        if (heapType === 'func' || heapType === 'funcref') {
            typeByte = TYPE.FUNCREF;
        } else if (heapType === 'extern' || heapType === 'externref') {
            typeByte = TYPE.EXTERNREF;
        } else {
            throw createError(instr, func, module, `Invalid heap type '${heapType}' for ref.null.`);
        }
        binary.push(typeByte);
        return true;
    }

    if (instr.type === 'ref.func') {
        if (instr.loc && instr.loc.line != null) {
            addMapping(instr.loc.line, instr.loc.column, binary.length);
        }
        binary.push(REF_FUNC_OPCODE);
        
        const funcIdentifier = instr.funcIdentifier || instr.func; // AST might use either
        if (funcIdentifier == null) {
             throw createError(instr, func, module, "ref.func requires a function index or name.");
        }

        let funcIndex = -1;
        if (typeof funcIdentifier === 'string' && funcIdentifier.startsWith('$')) {
            funcIndex = module.functions.findIndex(f => f.name === funcIdentifier);
            if (funcIndex === -1) {
                throw createError(instr, func, module, `Unknown function reference: ${funcIdentifier}`);
            }
        } else {
            funcIndex = Number.parseInt(funcIdentifier, 10);
            if (Number.isNaN(funcIndex) || funcIndex < 0 || funcIndex >= module.functions.length) {
                 throw createError(instr, func, module, `Invalid function index: ${funcIdentifier}`);
            }
        }
        binary.push(...encodeULEB128(funcIndex));
        return true;
    }

    if (instr.type === 'ref.is_null') {
        if (instr.loc && instr.loc.line != null) {
            addMapping(instr.loc.line, instr.loc.column, binary.length);
        }
        binary.push(REF_IS_NULL_OPCODE);
        return true;
    }

    return false;
}