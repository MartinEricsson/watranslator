// Generated by 🤖
import wasmConstants from "../constants.mjs";
import {
    encodeULEB128,
    encodeString
} from "../compile-utils.mjs";
import { addMapping, setSectionBaseOffset } from "../../sourcemap.mjs";
import { createError } from "../instructions/error.mjs";


const {
    EXPORT_KIND, // Assuming EXPORT_KIND map is similar to IMPORT_KIND for { FUNC, TABLE, MEM, GLOBAL }
    SECTION,
} = wasmConstants; // EXPORT constant might be EXPORT_KIND if it holds the numeric values

export function exportSection(module, binary) {
    if (!module.exports || Object.keys(module.exports).length === 0) {
        return;
    }

    const sourceMapEntries = [];
    const exportSectionContentBytes = [];
    
    const exportsArray = Object.entries(module.exports); // [name, data_obj]

    exportSectionContentBytes.push(...encodeULEB128(exportsArray.length)); // Count of exports

    for (const [exportName, exportData] of exportsArray) {
        // exportData is the AST node from parseExport, e.g. 
        // { type: 'export_decl', name, nameLoc, kind, kindLoc, index, indexLoc, exportDescLoc, loc }
        // The `exportData.loc` refers to the `(export ...)` s-expression's start or 'export' keyword.
        if (exportData.loc && exportData.loc.line != null) {
            sourceMapEntries.push({ loc: exportData.loc, relativeOffset: exportSectionContentBytes.length });
        }

        const nameBytes = encodeString(exportName); // exportName is the string key
        exportSectionContentBytes.push(...nameBytes);

        let exportKindByte;
        switch (exportData.kind) {
            case 'func': exportKindByte = EXPORT_KIND.FUNCTION; break;
            case 'table': exportKindByte = EXPORT_KIND.TABLE; break;
            case 'memory': exportKindByte = EXPORT_KIND.MEMORY; break;
            case 'global': exportKindByte = EXPORT_KIND.GLOBAL; break;
            default:
                throw createError(exportData, null, module, `Unknown export kind: ${exportData.kind}`);
        }
        exportSectionContentBytes.push(exportKindByte);

        // The index should have been resolved by the parser or main compile phase
        // from $name to actual numeric index.
        let itemIndex;
        if (typeof exportData.index === 'number') {
            itemIndex = exportData.index;
        } else if (typeof exportData.index === 'string' && !exportData.index.startsWith('$')) {
            itemIndex = parseInt(exportData.index, 10);
        } else {
             // If index is still a string like '$name', it needs to be resolved.
             // This resolution should happen before or at the start of this section compilation.
             // For now, assuming it's resolved to a number.
             throw createError(exportData, null, module, `Export index for '${exportName}' is not a resolved number: ${exportData.index}`);
        }
        if (Number.isNaN(itemIndex)) {
             throw createError(exportData, null, module, `Invalid numeric index for export '${exportName}': ${exportData.index}`);
        }
        exportSectionContentBytes.push(...encodeULEB128(itemIndex));
    }

    if (exportSectionContentBytes.length === encodeULEB128(exportsArray.length).length && exportsArray.length === 0) {
        return;
    }

    const sectionStartOffsetInBinary = binary.length;
    binary.push(SECTION.EXPORT);
    const sectionSizeBytesArray = encodeULEB128(exportSectionContentBytes.length);
    binary.push(...sectionSizeBytesArray);
    
    setSectionBaseOffset(sectionStartOffsetInBinary + 1 + sectionSizeBytesArray.length);
    
    for (const entry of sourceMapEntries) {
        addMapping(entry.loc.line, entry.loc.column, entry.relativeOffset);
    }
    
    binary.push(...exportSectionContentBytes);
    setSectionBaseOffset(0);
}