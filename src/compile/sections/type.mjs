// Generated by 🤖
import wasmConstants from "../constants.mjs";
import { encodeULEB128, getWasmType } from "../compile-utils.mjs";
import { createError } from "../instructions/error.mjs"; // Path might need adjustment if error.mjs is not in instructions
import { addMapping, setSectionBaseOffset } from "../../sourcemap.mjs";
const {
    SECTION, TYPE // Assuming TYPE.FUNCTYPE (0x60) is in wasmConstants
} = wasmConstants;

// compileFunctionType remains a local helper, does not call addMapping directly
function compileFunctionType(funcOrBlockType, isBlockType = false) {
    const typeBytes = [TYPE.FUNCTYPE]; // Function type tag 0x60

    const params = isBlockType ? [] : (funcOrBlockType.parameters || []);
    const results = isBlockType ? (funcOrBlockType.types || []) : (funcOrBlockType.results || []);

    const paramTypes = params.map(param => getWasmType(param.type || param.value)); // param.value if it's {value, loc}
    typeBytes.push(...encodeULEB128(paramTypes.length));
    typeBytes.push(...paramTypes);

    let resultWasmTypes = [];
    try {
        if (results && Array.isArray(results)) {
            // Results might be simple strings or {value, loc} objects from parser
            resultWasmTypes = results.map(r => getWasmType(r.value !== undefined ? r.value : r));
        }
    } catch (e) {
        // Create error expects an AST instruction node. Pass a mock or improve createError.
        // For now, rethrow with more context.
        const originName = isBlockType ? "block type" : (funcOrBlockType.name || "anonymous function");
        throw new Error(`Error compiling result types for ${originName}: ${e.message}`);
    }

    typeBytes.push(...encodeULEB128(resultWasmTypes.length));
    typeBytes.push(...resultWasmTypes);

    return typeBytes;
}

export function typeSection(functions, multiValueBlockTypes, binary) {
    if (!functions || (!multiValueBlockTypes && functions.length === 0)) { // Corrected condition
        return;
    }
    if (!multiValueBlockTypes) multiValueBlockTypes = []; // Ensure it's an array

    const allTypeSources = []; // To store { loc, relativeOffset } pairs

    // This will hold the actual byte content of the type section (vector of type entries)
    const typeSectionContentBytes = []; 

    const numTypes = functions.length + multiValueBlockTypes.length;
    if (numTypes === 0) return;

    typeSectionContentBytes.push(...encodeULEB128(numTypes)); // Vector count

    // Function type entries
    for (const func of functions) {
        if (func.loc && func.loc.line != null) {
            // Record loc and current offset *within typeSectionContentBytes*
            allTypeSources.push({ loc: func.loc, relativeOffset: typeSectionContentBytes.length });
        }
        typeSectionContentBytes.push(...compileFunctionType(func, false));
    }

    // Add types for multi-value blocks
    for (let i = 0; i < multiValueBlockTypes.length; i++) {
        const blockType = multiValueBlockTypes[i]; // blockType = { types: string[], instructions: ASTNode[], loc?: Location }
                                                 // loc here should be the loc of the block/loop/if AST node.
        blockType.typeIndex = functions.length + i; // This assignment is fine.

        if (blockType.loc && blockType.loc.line != null) {
            allTypeSources.push({ loc: blockType.loc, relativeOffset: typeSectionContentBytes.length });
        }
        typeSectionContentBytes.push(...compileFunctionType(blockType, true));
        
        // Assign the type index to all instructions using this type
        // This part should remain, as it's about linking typeIndex in AST
        if (blockType.instructions) { // Ensure instructions exist
           for (const instr of blockType.instructions) {
               instr.typeIndex = blockType.typeIndex;
           }
        }
    }
    
    // Now that typeSectionContentBytes is complete, write the section to the main binary
    const typeSectionStartOffsetInBinary = binary.length;
    binary.push(SECTION.TYPE);
    const sectionSizeBytesArray = encodeULEB128(typeSectionContentBytes.length);
    binary.push(...sectionSizeBytesArray);
    
    // Set the base offset for addMapping calls to be the start of the content
    setSectionBaseOffset(typeSectionStartOffsetInBinary + 1 + sectionSizeBytesArray.length);
    
    // Call addMapping for all recorded items
    for (const typeSource of allTypeSources) {
        addMapping(typeSource.loc.line, typeSource.loc.column, typeSource.relativeOffset);
    }
    
    binary.push(...typeSectionContentBytes); // Append the actual content
    setSectionBaseOffset(0); // Reset for the next section
}