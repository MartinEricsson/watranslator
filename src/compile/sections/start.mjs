// Generated by 🤖
import wasmConstants from "../constants.mjs";
import { encodeULEB128 } from "../compile-utils.mjs";
import { addMapping, setSectionBaseOffset } from "../../sourcemap.mjs";
import { createError } from "../instructions/error.mjs";


const {
    SECTION,
} = wasmConstants;

export function startSection(module, functions, binary) {
    // module.start is an object like { value: funcIdOrName, loc: {line, column} }
    if (module.start && module.start.value != null) { 
        let startFuncIndex;
        const startValue = module.start.value;

        if (typeof startValue === 'string' && startValue.startsWith('$')) {
            startFuncIndex = functions.findIndex(f => f.name === startValue);
            if (startFuncIndex === -1) {
                throw createError({ loc: module.start.loc }, null, module, `Unknown start function name: ${startValue}`);
            }
        } else if (typeof startValue === 'number' || /^\d+$/.test(startValue)) {
            startFuncIndex = Number.parseInt(String(startValue), 10);
            if (Number.isNaN(startFuncIndex) || startFuncIndex < 0 || startFuncIndex >= functions.length) {
                 throw createError({ loc: module.start.loc }, null, module, `Invalid start function index: ${startValue}`);
            }
        } else {
            throw createError({ loc: module.start.loc }, null, module, `Invalid start function identifier: ${startValue}`);
        }

        const startSectionContentBytes = [...encodeULEB128(startFuncIndex)];

        const sectionStartOffsetInBinary = binary.length;
        binary.push(SECTION.START);
        const sectionSizeBytesArray = encodeULEB128(startSectionContentBytes.length);
        binary.push(...sectionSizeBytesArray);
        
        // The mapping for the (start ...) directive should point to where its content (the func index) is written.
        if (module.start.loc && module.start.loc.line != null) {
            setSectionBaseOffset(sectionStartOffsetInBinary + 1 + sectionSizeBytesArray.length);
            addMapping(module.start.loc.line, module.start.loc.column, 0); // Offset 0 within the content
            setSectionBaseOffset(0);
        }
        
        binary.push(...startSectionContentBytes);
    }
}