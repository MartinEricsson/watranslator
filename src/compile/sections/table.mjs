// Generated by 🤖
import wasmConstants from "../constants.mjs";
import { encodeULEB128, getWasmType } from "../compile-utils.mjs"; // getWasmType might be useful for table.type
import { addMapping, setSectionBaseOffset } from "../../sourcemap.mjs";
import { createError } from "../instructions/error.mjs";


const {
    SECTION,
    TYPE
} = wasmConstants;

export function tableSection(tables, binary) {
    if (!tables || tables.length === 0) {
        return;
    }

    const sourceMapEntries = [];
    const tableSectionContentBytes = [];

    tableSectionContentBytes.push(...encodeULEB128(tables.length)); // Count of tables

    for (const table of tables) {
        // table AST node should have: id, idLoc, min, minLoc, max, maxLoc, tableType, typeLoc, loc
        // We map the table.loc to the start of its definition here.
        if (table.loc && table.loc.line != null) {
            sourceMapEntries.push({ loc: table.loc, relativeOffset: tableSectionContentBytes.length });
        }

        // Determine table element type (reftype)
        const tableReftype = table.tableType || table.type || 'funcref'; // AST might use 'tableType' or 'type'
        try {
            tableSectionContentBytes.push(getWasmType(tableReftype));
        } catch (e) {
            throw createError(null, null, { tables: module.tables }, `Invalid table reftype: ${tableReftype}`);
        }
        
        // Limits
        const minPages = table.min != null ? table.min : 0; // Default min 0 if not specified
        const maxPages = table.max; // Can be null or undefined

        let flags = 0;
        if (maxPages != null) {
            flags |= 0x01; // Bit 0 indicates max is present
            // Bit 1 (0x02) for shared table, Bit 2 (0x04) for 64-bit table index (not MVP)
        }
        tableSectionContentBytes.push(flags);
        tableSectionContentBytes.push(...encodeULEB128(minPages));

        if (maxPages != null) {
            if (maxPages < minPages) {
                 throw createError(null, null, { tables: module.tables }, `Table max pages (${maxPages}) < min pages (${minPages}) for table ${table.id || ''}`);
            }
            tableSectionContentBytes.push(...encodeULEB128(maxPages));
        }
    }

    if (tableSectionContentBytes.length === encodeULEB128(tables.length).length && tables.length === 0) {
        // Only contains the count of tables (which is 0)
        return;
    }

    const sectionStartOffsetInBinary = binary.length;
    binary.push(SECTION.TABLE);
    const sectionSizeBytesArray = encodeULEB128(tableSectionContentBytes.length);
    binary.push(...sectionSizeBytesArray);
    
    setSectionBaseOffset(sectionStartOffsetInBinary + 1 + sectionSizeBytesArray.length);
    
    for (const entry of sourceMapEntries) {
        addMapping(entry.loc.line, entry.loc.column, entry.relativeOffset);
    }
    
    binary.push(...tableSectionContentBytes);
    setSectionBaseOffset(0);
}